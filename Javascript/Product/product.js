//Given an array of distinct elements and a number x, find if there is a pair with product equal to x.

function product(value, array) {

    return [0, 0];
}

var x = 45;
const values = [1,5,15,18,46,9];
console.log(product(x, values));